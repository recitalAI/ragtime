server {
    listen 80;

    location /api/ {
        proxy_pass http://ragtime:5000/api/;
        proxy_set_header Host $host;
        proxy_read_timeout 120s;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;  # SPA history mode
    }
}
